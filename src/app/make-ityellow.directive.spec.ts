import { MakeItyellowDirective } from './make-ityellow.directive';

describe('MakeItyellowDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeItyellowDirective();
    expect(directive).toBeTruthy();
  });
});
