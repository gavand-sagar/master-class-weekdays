import { MytoggleDirective } from './mytoggle.directive';

describe('MytoggleDirective', () => {
  it('should create an instance', () => {
    const directive = new MytoggleDirective();
    expect(directive).toBeTruthy();
  });
});
